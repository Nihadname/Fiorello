<section id="products">
    <div class="container">
        <div class="row" id="ProductList">
            <input type="hidden" value="@ViewBag.AllCount" id="ProductCount2" />
            @await Component.InvokeAsync("Product", new { take = 4 })

        </div>
        <div class="d-flex justify-content-center">
            <button class="btn btn-primary SecondLoadMore">Load more</button>

        </div>
    </div>
</section>
